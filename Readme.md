# Public Ip with python

## Build Setup

```bash
# install setuptools
$ sudo apt-get install python3-setuptools

# install dependencies
$ sudo python3 setup.py install
```
For detailed explanation on how things work, check out the [documentation](https://google.com).
